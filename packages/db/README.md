# @ppu/db (placeholder)

Prisma schema (multi-file, one file per domain per `docs/adr/004-technology-decision-record.md`), generated client, and seed scripts implementing `docs/06-data-model.md`. Structural placeholder only — no schema yet; schema is added incrementally, one domain at a time, by the story that first needs persistence for that domain (vertical-slice delivery rule in `CLAUDE.md`).

Owning story: MVP-002 (Identity/session tables) adds the first schema file. See `docs/13-implementation-readiness-plan.md` §1 and §4.
