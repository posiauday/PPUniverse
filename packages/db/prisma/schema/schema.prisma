generator client {
  provider = "prisma-client"
  output   = "../../src/generated/client"
}

// Connection URL comes from prisma.config.ts (Prisma 7 removed
// datasource.url from the schema file — docs/adr/004, MVP-002 amendment).
datasource db {
  provider = "postgresql"
}
