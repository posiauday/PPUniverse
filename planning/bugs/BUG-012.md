---
id: BUG-012
title: "[BUG] A production build opens a new Postgres connection pool for every database query (@ppu/db does not cache its client when NODE_ENV=production)"
severity: P1
related_stories: MVP-002 (@ppu/db); affects every database-backed page and API route
related_requirement: n/a (reliability; no requirement row covers connection handling)
status: Fix approved (option A, 2026-09-21); fix in PR #6, pending CI
found_date: 2026-09-21
planned_fix: option A approved 2026-09-21: the validated one-condition fix, as one minimal commit in MVP-023 (PR #6)
---

Follows `planning/github/04-bug-template.md`. Found while implementing MVP-023.

## Summary
`packages/db/src/index.ts` (delivered by MVP-002) only caches its Prisma client on `globalThis` when `NODE_ENV !== "production"`:

```ts
function getPrisma(): PrismaClient {
  if (globalThis.__ppuPrisma) return globalThis.__ppuPrisma;
  const client = createPrismaClient();
  if (process.env["NODE_ENV"] !== "production") {
    globalThis.__ppuPrisma = client;
  }
  return client;
}
```

The exported `prisma` is a lazy Proxy that calls `getPrisma()` on **every property access**. In a production build nothing is ever cached, so every query constructs a new `PrismaClient` with a new `pg` pool. Connections are only closed after the pool's idle timeout (10 s by default), so a burst of requests leaves one live connection per query and Postgres refuses new ones at its `max_connections` (100 by default) with "sorry, too many clients already" (Prisma `P2037` / `TooManyConnections`). The affected requests return HTTP 500. In development and test the client is cached, which is why nothing caught this before: tests run with `NODE_ENV=test` and `next dev`, and no production deployment exists yet.

## Severity
- [x] P1 — checkout or download outage (any database-backed page or API would fail under modest concurrent load in production)

No production deployment exists, so nothing has been exposed.

## Affected requirement / feature
- Reliability of every database-backed route (catalog, product, search, sign-in, sessions, health). Delivered by MVP-002.

## Environment
- Production build (`next build` then `next start`, Next.js 16.3.5, `NODE_ENV=production`); Postgres 17 with default settings.

## Steps to reproduce
1. Run against a local Postgres, from `packages/e2e`: a script that imports `@ppu/db`, runs 40 sequential `prisma.category.count()` calls, then counts `pg_stat_activity` rows for the database.
2. Run it with `NODE_ENV=development`, then with `NODE_ENV=production`.

## Expected behavior
One shared client and a bounded pool regardless of `NODE_ENV`.

## Actual behavior
Measured 2026-09-21:
- `NODE_ENV=development`: 40 queries, **1** open connection, client cached on `globalThis`.
- `NODE_ENV=production`: 40 queries, **41** open connections, client not cached.

## Evidence
The first CI run of the MVP-023 accessibility job (run 35665652208) served the production build to three parallel browser workers and failed three Chromium tests (Firefox and WebKit passed every test): an HTTP **500** where a 404 was expected, a category page with no products, and a session revoke whose DELETE failed. The server log for that job shows "Too many database connections opened: sorry, too many clients already" with Prisma `P2037` and `DriverAdapterError: TooManyConnections` on `prisma.category.findUnique`, `prisma.product.findFirst` and `prisma.session.delete`. The accessibility tests are not at fault; they exposed the defect.

## Security or privacy impact
- [x] Availability: an unauthenticated request rate can exhaust the database's connections and make every database-backed route return 500. No data exposure.

## Accessibility impact
- [x] None directly.

## Suggested fix
Cache the client on `globalThis` in every environment (drop the `NODE_ENV` condition in `getPrisma`), and bound the pool explicitly. Add a regression test that fails before the fix: a unit test that, with `NODE_ENV=production`, two accesses to `prisma` construct the client once; and a database-gated integration check that many sequential queries hold at most a pool's worth of connections.

## Why it is not fixed in MVP-023
Changing `@ppu/db` changes MVP-002 behavior, which the MVP-023 authorization reserves (only the Q37 fixes may touch completed stories). Recorded as open question 43; MVP-023 is Blocked until the product owner decides.
## Update (2026-09-21)
- CI run 2 (35666983338, docs-only head 9cd68c5): the same signature. 417 of 420 passed, 3 Chromium failures (an unknown category slug and an unknown product slug returned 500 instead of 404; a session revoke failed), 0 skipped, 0 retries; Firefox and WebKit passed every test; the server log again shows TooManyConnections (8 lines). Whole job 5m57s, test execution 4m14s. The Playwright browser cache is saved only by a successful job, so both runs so far had a cold cache.
- Proposed fix validated in a throwaway git worktree (never committed, not on the feature branch): one condition in packages/db/src/index.ts (cache the client on globalThis in every environment) plus a unit test (packages/db/src/client-cache.test.ts). The test fails without the fix in production mode (and passes in development and test), and passes with it; with the fix, 40 sequential queries in production mode hold 1 open connection (was 41); and the full accessibility suite against a production build with the fix: 420 passed, 0 failed, 0 skipped, 0 retries (140 per engine) in 7.0m on a local Windows run with 3 workers; TooManyConnections lines in the server log: 0.
### Validated patch (NOT applied to any branch; apply as one separate commit once approved)

```diff
diff --git a/packages/db/src/client-cache.test.ts b/packages/db/src/client-cache.test.ts
new file mode 100644
index 0000000..91b8280
--- /dev/null
+++ b/packages/db/src/client-cache.test.ts
@@ -0,0 +1,47 @@
+import { afterEach, describe, expect, it, vi } from "vitest";
+
+// BUG-012: the exported `prisma` is a lazy proxy that resolves the real client on every
+// property access. The client was only cached when NODE_ENV !== "production", so a
+// production build constructed a new PrismaClient (and a new pg pool) for every query,
+// exhausting Postgres's connections under concurrent load.
+const constructed = vi.fn();
+
+vi.mock("@prisma/adapter-pg", () => ({
+  PrismaPg: class {
+    constructor(public readonly options: unknown) {}
+  },
+}));
+
+vi.mock("./generated/client/client.js", () => ({
+  PrismaClient: class {
+    readonly category = { count: async () => 0 };
+    constructor(options: unknown) {
+      constructed(options);
+    }
+  },
+}));
+
+describe("the shared Prisma client is constructed once, whatever NODE_ENV is", () => {
+  afterEach(() => {
+    vi.unstubAllEnvs();
+    delete (globalThis as { __ppuPrisma?: unknown }).__ppuPrisma;
+    vi.resetModules();
+    constructed.mockClear();
+  });
+
+  it.each(["production", "development", "test"])(
+    "in %s mode, repeated use builds one client and one pool",
+    async (mode) => {
+      vi.stubEnv("NODE_ENV", mode);
+      vi.stubEnv("DATABASE_URL", "postgresql://user:pw@localhost:5432/db");
+      vi.resetModules();
+
+      const { prisma } = await import("./index.js");
+      for (let query = 0; query < 5; query++) {
+        await prisma.category.count();
+      }
+
+      expect(constructed).toHaveBeenCalledTimes(1);
+    },
+  );
+});
diff --git a/packages/db/src/index.ts b/packages/db/src/index.ts
index 2f9432a..2279ddc 100644
--- a/packages/db/src/index.ts
+++ b/packages/db/src/index.ts
@@ -24,9 +24,10 @@ function getPrisma(): PrismaClient {
     return globalThis.__ppuPrisma;
   }
   const client = createPrismaClient();
-  if (process.env["NODE_ENV"] !== "production") {
-    globalThis.__ppuPrisma = client;
-  }
+  // Cached in every environment. The exported proxy below calls getPrisma() on every
+  // property access, so skipping the cache in production built a new client and a new
+  // connection pool for every query and exhausted Postgres (BUG-012).
+  globalThis.__ppuPrisma = client;
   return client;
 }
```
## Decision (2026-09-21, direct product-owner instruction): option A
The product owner approved applying the validated one-condition fix in `@ppu/db` inside MVP-023. It is an approved change to delivered code under Q37 and the **only** `@ppu/db` change permitted in this story (`docs/final-decisions.md`, "Product-owner decision: BUG-012 and MVP-023 sign-off"; open question 43 is closed).

- **Classification: (i) a root fix, not (ii) a mitigation.** The patch corrects the client and connection *lifecycle*: a production build constructed a new Prisma client and a new `pg` pool on every query; it now constructs one per process and reuses it. It raises no connection limit, sets no pool option and leaves `pg`'s default pool size unchanged, so no underlying leak remains and no tech-debt record is required for one. (Had it raised a limit or ceiling it would be a mitigation and would need a tech-debt record for the leak; it does not.)
- **Change:** one condition in `getPrisma()` in `packages/db/src/index.ts` (the `NODE_ENV !== "production"` guard is removed, so the client is always cached on `globalThis`) plus one new unit test, `packages/db/src/client-cache.test.ts`. One minimal commit referencing BUG-012; no refactoring, renaming or tidying of adjacent code.
- **Regression test:** the condition is unit-testable, so there is one. It fails in production mode without the fix (development and test pass) and passes with it; it is shown failing on the feature branch before the fix is applied.
- **Confirmed unchanged:** RLS behavior (the policies are database-side and no client code sets a role or a session setting); authorization; credential handling (`DATABASE_URL` is read and passed to the adapter exactly as before; `createPrismaClient` is untouched); no connection string or secret surface added or changed.
- **One consequence:** in production the process now reads `DATABASE_URL` once, on first use, as development and test already did, so a rotated credential needs a process restart.
- **Sequence (not reordered):** push the fix, wait for CI, read both job logs in full, report the accessibility runtime, only then create the `develop` protection rule, then the security and accessibility reviews, then Done, then `gh pr merge`. If CI is not green after the patch, stop and report; the suite configuration, worker count, retries and timeouts are not iterated on to force a pass.
