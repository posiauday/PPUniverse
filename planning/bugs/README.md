# Bug records

One file per bug: `BUG-XXX.md`, using the fields in `planning/github/04-bug-template.md`. `planning/bugs.csv` is a lightweight index (ID, title, severity, related story/requirement, status) kept in sync with the files here — the `.md` file is the authoritative record.

No bugs have been filed yet.
