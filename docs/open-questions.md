# Open Questions and Decision Log

1. **PARTIALLY RESOLVED (2026-09-24, direct product-owner instruction — see `docs/final-decisions.md`, "Product name (open question 1, partial)").** Product name: **LowCodeStacks**. Domain: **lowcodestacks.com** (registered). Implemented in the six affected files (`apps/web/lib/seo/site.ts`'s `SITE_NAME`, `packages/e2e/src/site.ts`'s paired copy, the three call sites that had re-hardcoded the old name, `README.md`), plus the test fixtures that asserted the old name as an expected value. **Still open, narrowed to trademark clearance only**: "LowCodeStacks" is descriptive (low conflict risk, correspondingly weak as a registrable/enforceable mark — accepted trade, not a defect). Close condition: a CIPO search and a clearance opinion, neither performed yet, neither urgent before a production deployment exists. `NEXT_PUBLIC_SITE_URL` needs no code change (already runtime config) but must be set to `https://lowcodestacks.com` once a production deployment first exists. *Original question:* Product name, domain and trademark clearance?
2. **CLOSED (2026-09-23, direct product-owner instruction — see `docs/final-decisions.md`, "Open question 2: invited and vetted third-party creators").** Invited and vetted third-party creators, not an open self-serve marketplace at MVP: apply → moderator review → approved to publish. A creator cannot publish without an approved application. *Original question:* First-party-only or invited third-party creators at MVP?
3. **NOT APPROVED — safest default narrowed, 2026-09-23, direct product-owner instruction.** Proposed launch scope narrowed to **Canada and the United States** only; EU and UK are additive later, once revenue justifies the registration overhead. Note: Stripe Tax *calculates* tax but does not remit it — registration obligations in each jurisdiction remain a separate, unresolved step. The MVP-010 `Download` record is noted as available chargeback evidence for a future "non-refundable once downloaded" refund position. **None of this — currency, tax position, jurisdictions, or refund policy — is decided.** *Original question:* Initial countries, currencies, taxes and refund obligations?
4. Identity provider and enterprise SSO requirements?
5. **PARTIALLY RESOLVED (2026-09-17, see `docs/final-decisions.md`)**: object storage vendor is Cloudflare R2 (production) / MinIO (development). Hosting region and data residency commitments themselves remain open.
6. **Part 1 CLOSED (2026-09-21, direct product-owner instruction — see `docs/final-decisions.md`)**: compatibility is modeled as one or more entries per product, each with a platform area (Power Apps, Power Automate, Power BI, Dataverse, Power Pages, Copilot Studio, Microsoft Fabric), a structured minimum supported release wave (release year + wave 1 or 2), and concise compatibility notes. **Part 2 CLOSED for the current MVP (2026-09-21, direct product-owner instruction — see `docs/final-decisions.md`, section B)**: the only assignable statuses are **Creator Declared** (assigned by creators) and **Marketplace Reviewed** (assigned by moderators after review; it does not mean independently tested, certified, guaranteed, Microsoft approved/certified, officially supported or verified compatible). **Tested is reserved** — not assignable by creators or moderators, never inferred, never migrated to, with no user-facing badge or filter. The future Tested program is deferred, not approved (item 27). Follow-ups: items 27 and 28. **Implementation note (MVP-005, 2026-09-21):** the model, display and write-time rules (`validateCompatibilityEntry`) are built, but no write path calls the validator yet and nothing screens notes/summaries for private data (`planning/tech-debt/TD-006.md`), and the built status vocabulary predates the Part 2 decision (`planning/tech-debt/TD-008.md`, item 28). The review of MVP-005's unspecified wording (empty states, release-wave explanation, legend heading) is CLOSED — approved as written.
7. **PARTIALLY RESOLVED (2026-09-18, see `docs/final-decisions.md`)**: license families are locked as Personal/Team/Enterprise. Exact pricing, seat limits, and contract terms per tier remain open. **Framing corrected (2026-09-23, direct product-owner instruction, NOT APPROVED):** a license tier is a grant attached to a purchased product (Catalog domain, `ProductLicense`) — not a subscription plan a buyer subscribes to. Price is set per product per tier (a creator can price the same product's Personal/Team/Enterprise grants differently). An `Entitlement` records which tier-grant a buyer holds for which product, not which "plan" they are on. The commission rate remains entirely open and requires explicit financial sign-off before any default is recorded.
8. **NOT APPROVED (2026-09-23, direct product-owner instruction).** Automated split payouts via a Connect-style integration were considered and **set aside as a reversal of the existing deferral in `docs/09-marketplace-operations.md`** ("Defer automated split payments until seller volume, tax/legal setup, identity verification and support operations justify the complexity. MVP may sell first-party products and manually onboard a limited creator cohort under approved commercial terms."). **The standing position is unchanged: manual payouts for a founding cohort**, with the creator application (open question 2's decision, constraint 2) capturing payout *intent* only — no live payment-provider onboarding in that story. **Not to be re-proposed without an explicit reversal decision.** *Original question:* Creator commercial terms and manual versus automated payout model?
9. **PARTIALLY RESOLVED (2026-09-17, see `docs/final-decisions.md`)**: malware-scanning approach is ClamAV for development (already implemented, MVP-006) with an asynchronous production scanning service to follow (production vendor and cost model still open — see item 15). File types and maximum sizes still open as a business decision; `packages/domain/files/src/upload-policy.ts`'s current 50MB/allow-listed-MIME-type policy remains an explicit, reversible technical default pending that decision.
10. Which MVP inventory is genuinely ready and owned for publication?
11. Which analytics categories require consent in launch jurisdictions?
12. Support ownership and published service targets?

## Technical decisions raised during implementation readiness planning (2026-09-16)
13. Final monorepo tooling confirmation (Turborepo vs. Nx) — default assumed is Turborepo; see `docs/adr/004-technology-decision-record.md`.
14. Redis/queue hosting (managed vs. self-hosted) and its region, once hosting region (item 5) is decided.
15. **PARTIALLY RESOLVED (2026-09-17)**: development malware-scanning vendor is ClamAV, already implemented. Production vendor and cost model for the async production scanning service (`docs/final-decisions.md`) still open — no story yet owns this.
16. **RESOLVED (2026-09-17, see `docs/final-decisions.md`)**: RLS is approved and required. Implemented as RLS-enabled-with-zero-policies on every table (portable standard PostgreSQL DDL, not dependent on Supabase-specific `auth.uid()`/PostgREST features) — closes `planning/tech-debt/TD-003.md`'s finding without affecting the app's own table-owner Postgres connection.
17. GitHub repository visibility, branch protection rules, and required status checks for issue/PR automation. **NARROWED, STILL OPEN (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner response to the MVP-023 stop-gate confirmation', item 1).** Decided and **applied (2026-09-21, confirmed live 2026-09-22 by reading the rule back via the GitHub API directly, not assumed)**: a branch-protection rule on `develop` only — pull request required, 0 approvals, required status checks = the existing job ("Format, lint, typecheck, test, build") and the accessibility job, branches need not be up to date, not enforced for administrators, force pushes and deletion blocked. This closes required status checks and force-push/deletion protection on `develop`. `Secret scan` is deliberately not a required check (`planning/bugs/BUG-014.md`, shelved gate-policy question — whether it should become one is unanswered). **Still open:** protection of `main`, reviewer and approval rules, any ruleset strategy, and repository visibility (tracked as TD-011).
18. **PARTIALLY RESOLVED (2026-09-17, see `docs/final-decisions.md`)**: error-monitoring/APM vendor is Sentry, product-analytics vendor is PostHog. Data-residency constraints tied to item 5 (hosting region) remain open.
19. **PARTIALLY RESOLVED (2026-09-17, see `docs/final-decisions.md`)**: email vendor is Resend. Data-residency constraints tied to item 5 (hosting region) remain open.
20. **CLOSED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner decisions for MVP-023', Q39).** Decided for the accessibility suite: a separate parallel CI job with a target of 5–8 minutes and a hard ceiling of 10 minutes wall-clock, measured time reported in the pull request. If the ceiling is exceeded the gate is not weakened; options (sharding, trimming redundant page/width combinations, a scheduled full-matrix run with a reduced required set) return as a new decision. A flaky accessibility test is a defect (no blanket retries; at most one visible retry for known network/boot flake). The TRD's pull-request check list is updated as part of MVP-023. *Original question:* CI time/cost budget for the full test suite (unit + integration + E2E + accessibility) and its effect on PR feedback latency. **Architecture evolved (2026-09-23, direct product-owner instruction — `docs/final-decisions.md`, 'Accessibility suite mitigation: sharding implemented').** The single-job wall-clock ceiling triggered per this decision's own stated policy (breached, MVP-018 merge entry) and then again on the test-execution measure. The CI job is now a 4-shard matrix plus an aggregator required check of the same name; the governing budget is per-shard test execution (target under 5 minutes, ceiling 8 minutes per shard), not job wall-clock. This item stays **CLOSED** — the underlying policy (never weaken engines/widths/rules to fit a budget; a breach triggers a new decision) is unchanged, only its enforcement mechanism evolved.

## Requirement coverage gaps found during traceability completion (2026-09-16)
21. **PARTIALLY RESOLVED (2026-09-18)**: product-owner confirmed FR-016 (funnel-event analytics, PostHog) stays out of MVP-022's scope — MVP-022 is operational observability only (logs, traces, correlation IDs, Sentry error monitoring). FR-016 still has no owning backlog story; a new story needs to be added to `planning/mvp-backlog.csv` when prioritized.
22. **CLOSED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner decisions for MVP-023', Q33, Q34 and Q42).** MVP-023 owns NFR-008: a browser matrix (chromium, firefox, webkit; pinned bundled builds) and a breakpoint matrix (320, 375, 768, 1280), documented durably and exercised by the accessibility suite for accessibility and rendering checks only — not a general cross-browser functional regression suite. Implementation and evidence are tracked in MVP-023. *Earlier text:* NFR-008 (documented, tested supported-browser/responsive-breakpoint matrix) has no dedicated story; MVP-023 covers accessibility but not a browser/breakpoint test matrix. Needs a product-owner decision on whether to fold into MVP-023's acceptance criteria or add a new story. **Update (2026-09-21, MVP-023 pre-work):** a default browser matrix (question 33) and breakpoint matrix (question 34) are proposed for the product owner's approval, and question 42 asks whether MVP-023 then owns NFR-008. Not decided. **CLOSED (2026-09-21): MVP-023 is Done; NFR-008 is Implemented with the documented and exercised browser/breakpoint matrix (`docs/14-accessibility-testing.md`; `planning/requirement-traceability.csv`).** **Architecture note (2026-09-23, `docs/final-decisions.md`, 'Accessibility suite mitigation: sharding implemented').** The browser/breakpoint matrix itself (chromium/firefox/webkit at 320/375/768/1280px) is unchanged and unreduced by CI sharding — every engine and width is preserved in every one of the 4 shards. Only how the suite is distributed across CI jobs changed, not what NFR-008 requires or what is exercised. This item stays **CLOSED**.
23. NFR-010 (data retention/deletion jobs configurable by data class) is only partially covered by MVP-020, which is scoped to consent and deletion *requests*, not scheduled retention-by-data-class jobs. Needs a decision on whether MVP-020's acceptance criteria should expand or a new story is required.
24. `docs/04-information-architecture.md`'s URL model lists `/collections/[slug]` and `/creators/[handle]`, and `Collection`/`CollectionItem` are named catalog entities (`docs/06-data-model.md`), but no backlog story owns building either page (found during MVP-003). Safest reversible default applied: neither was built as part of MVP-003, which stays scoped to exactly its documented acceptance criteria (category + product listing pages). Needs a product-owner decision on whether to fold into an existing story or add a new one. **Update (2026-09-21):** creator identity and creator-profile *ownership* are assigned to MVP-011 (item 26); the public creator route and the `Collection` pages remain unresolved and must not be built unless approved. **Update (2026-09-23, direct product-owner instruction — `docs/final-decisions.md`, "MVP-017 / `/collections/[slug]` scope conflict: decided by direct product-owner instruction").** MVP-017 (FR-014) is scoped to tutorials, patterns, learning paths and comparison pages only (`/learn/[slug]`); `/collections/[slug]` and the `Collection`/`CollectionItem` entities stay explicitly excluded from MVP-017 and remain governed by this question exactly as before — **still unresolved, still must not be built unless separately approved.** This update records that MVP-017 is not the story that resolves this question; it does not close the question itself.
25. FR-002 lists "AI" among its filter dimensions, but the locked MVP taxonomy (`docs/final-decisions.md`, 2026-09-17) has six asset categories and excludes "AI Architect". It is undecided whether "AI" is a catalog category, a filter, or a reference to something such as Copilot Studio products (Copilot Studio is now an approved compatibility platform area, 2026-09-21, which is a different concept from a category). Found while correcting FR-002's traceability (`planning/tech-debt/TD-005.md`). Not resolved; needs a product-owner decision.
26. FR-003 lists product-page content that no backlog story delivers: creator, screenshots, demo, price, prerequisites, setup instructions, accessibility statement, changelog, version history and related assets. MVP-005's acceptance criteria cover only license/version/support/compatibility (found while completing MVP-005; `planning/tech-debt/TD-007.md`). **DISPOSITION RECORDED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, section C).** MVP-005 and MVP-021 are not expanded. Creator → MVP-011 (identity and profile *ownership*; public creator routing stays unresolved under item 24). Price → MVP-007, together with the pricing, currency, tax and refund decisions required first; no offers in structured data until priced and approved. Screenshots and demo → proposed *Product Media and Screenshots* story (the allowed demo format needs a later decision). Prerequisites and setup → proposed *Product Documentation and Prerequisites* story. Accessibility statement → proposed *Product Accessibility Disclosure* story (MVP-023 owns platform accessibility validation, not creator-product declarations). Changelog and version history → proposed *Product Releases and Changelog* story. Related assets → deferred until real inventory exists and a relationship rule is approved. The proposals are in `planning/proposed-stories.md` with status **Proposed** — not approved, not Ready, not on the board until the product owner directly approves each. FR-003 stays Partially Implemented. **Update (2026-09-21, MVP-023 decision Q32):** the proposals register also holds PROP-006 (the BUG-002 corrective story, FR-002, Proposed); see item 32.

## Raised 2026-09-21 (MVP-005 close-out and MVP-021 pre-work)
27. **DEFERRED — not approved.** The future "Tested" evidence program. Before Tested can be used the product owner must define: who performs testing; the required test procedure; required environment information; evidence retention; retesting frequency; expiration rules; the version and release-wave relationship; moderator responsibilities; public wording; and liability and disclaimer language (`docs/final-decisions.md`, section B).
28. **DISPOSITION RECORDED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, "Product-owner decisions for MVP-021").** TD-008 is **not** folded into MVP-021: it is a separate small corrective change that must land before MVP-012 lets creators or administrators write compatibility evidence through the product editor (`planning/tech-debt/TD-008.md`, still Open). Only Creator Declared and Marketplace Reviewed are assignable; Marketplace Reviewed is a per-claim evidence status. **"Not Verified" is not an assignable persisted status**: unreviewed information is Creator Declared, absent information shows the approved empty state (no Not Verified record is created), and any existing Not Verified enum/database value is legacy or reserved — not removed destructively and not converted without inspecting provenance. A nullable reviewed-at timestamp is approved for compatibility claims: null for Creator Declared, set only by the trusted server-side moderation workflow on transition to Marketplace Reviewed, never accepted from a client, cleared (returning the claim to Creator Declared) if the creator materially changes a reviewed claim, and never fabricated for existing records. A moderator identity or review reference may be proposed in TD-008 but is not added without reviewing the moderation schema and approved scope. **Still open (for TD-008 / MVP-013):** how, and with what wording, a reviewed date is shown publicly; what counts as a "material" change; administrator authority over compatibility evidence; whether a moderator may withdraw a review.
29. **CLOSED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`).** A category with zero PUBLISHED products is not indexed (`noindex, follow`) and is excluded from `sitemap.xml`; it stays publicly reachable and does not return 404 merely because it is empty. Only PUBLISHED products count (never DRAFT, suspended, rejected, archived or unpublished). Indexability is derived from current published inventory, not from a manually edited flag.
30. **CLOSED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`).** Approved parameter-specific canonical policy: the base category page is `index, follow` with a self-canonical; a valid `?page=N` (N ≥ 2) is `index, follow` with a self-canonical to that exact URL, and `?page=1` is normalized to the base URL; invalid, zero, negative, non-numeric or out-of-range `page` values never become an indexable duplicate; `?q=`, sort-only, filter and mixed-parameter variants are `noindex, follow` with the clean base canonical and are excluded from the sitemap. The sitemap lists base category pages only. This is an intentional corrective SEO change owned by MVP-021, not a reopening of MVP-004; MVP-004's search and filtering behavior is unchanged.
31. **CLOSED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`).** Product JSON-LD ships now, without Offer data, using only fields backed by real PUBLISHED product data. Price and Offer data must not be added until pricing, currency, tax and checkout decisions are approved and implemented. No rich-result eligibility claim is made.
32. **DECIDED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner decisions for MVP-023', Q32).** Out of scope for MVP-023 and not fixed there. Approved as its own small corrective story, recorded as PROP-006 in `planning/proposed-stories.md` (Proposed; FR-002; P3; reference BUG-002; sequenced after MVP-023 and before any story that expands search). Not started; it stays Proposed until scheduled. A URL shape the accessibility harness crawls that triggers BUG-002 is excluded from the gate and noted. *Earlier text:* Repeated query parameters crash search (BUG-002, found 2026-09-21 while verifying MVP-021): `normalizeQuery` calls `.trim()` on a value Next.js passes as an array, so `/search?q=a&q=b` and `/categories/<slug>?q=a&q=b` return HTTP 500. It is MVP-004 code and the MVP-021 authorization forbids changing MVP-004 search behavior. Needs a product-owner decision on whether to approve a small corrective change (safest reversible default: none is made; a repeated `q` should degrade to the first value or to no query, with a unit test). **MVP-023 pre-work (2026-09-21):** the suggested handling is to leave it untouched and record it as out of scope for MVP-023. Open: should BUG-002 become a separate small corrective story after MVP-023? Recommendation: yes (the fix is tiny, but it changes MVP-004 search behavior, so it needs approval as its own story).

## Raised 2026-09-21 (MVP-023 pre-work) — decided by the product owner on 2026-09-21 except item 38 (open, interim position recorded); see `docs/final-decisions.md`
33. **DECIDED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner decisions for MVP-023', Q33).** Chromium, Firefox and WebKit as three Playwright projects (Edge is covered by Chromium; no branded Edge channel in the blocking gate). Pinned bundled browser builds with `@playwright/test` pinned exactly; no moving or branded channel may gate a PR (exploratory, non-blocking runs only). The original instruction said 'four engines'; the product owner confirmed on 2026-09-21 that this was a counting error and the three named engines are the decision (no engine was removed). *Original question:* **Browser matrix for the accessibility gate (Q-22A).** Safest reversible default: Chrome, Edge and Firefox, latest stable. Approve, or supply a different matrix (for example add Safari/WebKit, the only engine on iOS)? Also: use pinned bundled builds for the blocking gate (reproducible, not "latest"), or moving channel builds ("latest", not reproducible across dates)?
34. **DECIDED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner decisions for MVP-023', Q34).** Widths 320 (reflow), 375, 768 and 1280, sampled either side of the Tailwind breakpoints 640 and 1024. No expansion in MVP-023; a defect at another width is recorded as a bug and a matrix change proposed separately. Horizontal overflow is checked at 320 px on every gated page. *Original question:* **Responsive test viewports (Q-22B).** Safest reversible default: Mobile 375, Tablet 768, Desktop 1280 plus a 320 px reflow check, using the repository's existing breakpoints (Tailwind defaults: `sm` 640 px and `lg` 1024 px; `docs/05` names Mobile/Tablet/Desktop without widths). Approve, or give specific viewport requirements?
35. **DECIDED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner decisions for MVP-023', Q35).** A story is not Done if automated accessibility tests fail or manual review finds a critical WCAG 2.2 A/AA violation. Critical = any A/AA failure that prevents, blocks or substantially impedes a step in a core user journey. Severity rubric P1–P4 (P1 and P2 block Done; P3 and P4 are recorded as bugs). axe rules tagged wcag2a, wcag2aa, wcag21a, wcag21aa and wcag22aa block; best-practice rules are advisory. No claim of WCAG compliance, accessibility, certification, audit or conformance anywhere. *Original question:* **Accessibility gate definition (Q-22C).** Safest reversible default: a story cannot be marked Done if automated accessibility tests fail, or if manual review finds a *critical* WCAG 2.2 AA violation (`docs/11` already lists "critical accessibility issue in core path" as a release blocker). Approve? Open: what "critical" means — proposal: any A/AA success-criterion failure that prevents or substantially impedes a core journey step; and whether axe best-practice findings block or are advisory (default: advisory).
36. **DECIDED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner decisions for MVP-023', Q36).** Approved as proposed: no fabricated inventory; rows are created by the test under a reserved non-production prefix and cleaned up by that prefix only; seeded categories are read-only; a pre-flight guard refuses any database it cannot positively identify as local or CI, and a refused test fails or skips loudly. *Original question:* **Accessibility test data (Q-22D).** Safest reversible default: no fabricated marketplace inventory; tests create isolated temporary rows with a reserved prefix and delete only those; seeded categories may be referenced but are never modified or deleted; a guard refuses to write to any database that is not local or CI. Approve?
37. **DECIDED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner decisions for MVP-023', Q37).** Fix the WCAG 2.2 A/AA failures inside MVP-023; no allowlist; estimate 8 to 13 points. In scope: BUG-003, BUG-004 (border and placeholder), BUG-005 and BUG-006 (error identification, focus, announcement, titles), BUG-007 (heading skip), BUG-008 (404 landmark and title). Out of scope and advisory: the unstyled sign-in/account appearance, best-practice findings, any P3/P4 found during implementation. Each fix is a separate minimal commit naming the bug ID and WCAG criterion, with a regression test that fails before the fix; no refactoring; stop and ask if a fix needs a structural change. *Original question:* **Baseline policy for measured accessibility defects in delivered pages** (BUG-003 to BUG-008: invisible focus ring on the Search button, low input/placeholder contrast, sign-in error identification and focus, session-revoke focus and announcement, page titles, skipped heading levels, 404 landmark). Options: fix the WCAG A/AA failures inside MVP-023 as separately documented minimal corrective commits (safest default and the only option consistent with the backlog's "core journeys meet the gate"); or ship the harness with a bug-referenced allowlist and fix in follow-up stories (leaves a non-blocking gate — tech debt). Which?
38. **OPEN — interim position binding (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner decisions for MVP-023', Q38).** Still needed: who performs manual review (product owner, a named reviewer or a contractor), the cadence (per UI story or per release), and which AT/browser pairs are in scope. Until answered, screen-reader compatibility is UNVERIFIED (no screen reader has been run) and no record may say otherwise; the accessibility documentation carries a prominent 'Not verified' section (screen readers NVDA, JAWS and VoiceOver, voice control, switch access, magnification, any browser/AT pairing). Recorded proposal, NOT approved: NVDA + Firefox (Windows) and VoiceOver + Safari (Apple), reviewed per release. MVP-023 builds the checklist and process document with reviewer, cadence and AT matrix as TBD fields. *Original question:* **Manual accessibility review and assistive technology.** Who performs manual review and screen-reader testing (product owner, a named reviewer, a contractor), how often (per story with UI changes, or per release), and on which AT/browser pairs (for example NVDA + Firefox, VoiceOver + Safari, JAWS + Chrome)? Until answered: keyboard, zoom and reflow reviews are done with evidence, and screen-reader verification is stated as **not performed** — Claude cannot operate a screen reader.
39. **DECIDED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner decisions for MVP-023', Q39).** A separate parallel job, required for merge once the Q37 fixes have landed in the same story (sequence: harness and fixes together in this PR, then make it required); target 5–8 minutes, hard ceiling 10 minutes wall-clock, measured time reported in the PR; if exceeded the gate is not weakened and options return as a new decision; the TRD check list is updated; a flaky accessibility test is a defect (no blanket retries). **Enforcement (2026-09-21 response, item 1): Option B.** After the accessibility job ran green in the MVP-023 PR, a branch-protection rule was created on `develop` only (pull request required, 0 approvals, both jobs required, force-push and deletion blocked, not enforced for administrators) and, as of MVP-023's merge (2026-09-22), confirmed live by reading it back directly. This narrows item 17; it does not close it. MVP-023 itself merged at 423/423 accessibility tests passing (CI run `aed24d8`, merge commit `ed9b09b`), zero skipped, zero retries — the story's own final numbers, for the record, since the fixes-then-required sequence this decision describes is what produced them. *Original question:* **CI blocking and budget (open question 20).** Safest reversible default: a separate parallel CI job, required for merge once the baseline is resolved, with measured job time reported in the pull request; the TRD's pull-request check list (which has no E2E or accessibility stage) is extended to include it. Approve, and what time budget is acceptable?
40. **DECIDED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner decisions for MVP-023', Q40).** Yes: `/account` and `/account/sessions` are in scope, authenticated through the real server-enforced path with database-created test sessions created and torn down by the test under the reserved-prefix rules. No product backdoor, bypass route or relaxed authorization; stop and ask if coverage seems to need loosening a check. (`/account` is not a page in the repository; only `/account/sessions` exists. Confirmed 2026-09-21: `/account` is covered as a 404 check only, with no page, redirect or route stub; if it becomes a real page, that story must add it to the gate.) *Original question:* **Authenticated route in scope?** `/account/sessions` is an implemented journey with interactive controls (revoke, sign out) but is not public. Safest reversible default: include it, tested with database-created test sessions and no product backdoor. Approve?
41. **DECIDED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner decisions for MVP-023', Q41).** Approved: `@playwright/test`, `axe-core` and `@axe-core/playwright` as exact-pinned devDependencies (versions and licences recorded from the installed packages; confirmed not shipped in the application bundle). ADR-004's status becomes Accepted, dated 2026-09-21, covering only the testing-stack rows (no other ADR row is approved by it — 2026-09-21 response, item 4); the testing row in `docs/final-decisions.md` names all three; Vitest is unchanged. *Original question:* **Tooling confirmation.** `docs/adr/004-technology-decision-record.md` lists "Vitest, Playwright (E2E), axe-core (automated a11y) + manual keyboard/screen-reader pass" but its status is still **Proposed**; `docs/final-decisions.md` names Playwright explicitly and "confirms" ADR-004 generally, but does not name axe-core. Confirm the testing row as approved (and update the ADR status), and confirm `@playwright/test`, `@axe-core/playwright` and `axe-core` at pinned versions — noting the two axe packages are MPL-2.0 (dev-only test dependencies, not shipped).
42. **CLOSED (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner decisions for MVP-023', Q42, with Q33 and Q34).** MVP-023 owns NFR-008 and closes item 22: documented browser and breakpoint matrices exercised by the accessibility suite for accessibility and rendering checks (not a general cross-browser functional regression suite); traceability maps NFR-008 to MVP-023. *Original question:* **NFR-008 ownership.** If questions 33 and 34 are approved, does MVP-023 own NFR-008 (documented, tested browser/breakpoint matrix), resolving item 22 by folding it into MVP-023 rather than adding a story?

Claude must not silently resolve these as facts. Use reversible defaults and record ADRs.

## Raised 2026-09-21 (MVP-023 implementation) — questions only; nothing here is decided
43. **DECIDED — option A (2026-09-21, direct product-owner instruction — `docs/final-decisions.md`, 'Product-owner decision: BUG-012 and MVP-023 sign-off').** The validated one-condition fix in `@ppu/db` is applied inside MVP-023 as one minimal commit with a regression test. It is the only `@ppu/db` change permitted in this story and it is a root fix (a correction to the client and connection lifecycle), not a mitigation; no limit or pool ceiling is raised. See `planning/bugs/BUG-012.md`. *Original question and evidence:* **BUG-012: the production Prisma client is not cached — how should it be fixed?** The first CI run of the MVP-023 accessibility job failed three Chromium tests with HTTP 500s caused by Postgres "too many clients" (Prisma `P2037`). In a production build `packages/db/src/index.ts` (MVP-002) creates a new client and connection pool on every query, reproduced locally on 2026-09-21: 40 sequential queries leave 41 open connections in production mode and 1 in development. This is a production reliability defect, not an accessibility one, and fixing it changes MVP-002 behavior, which the MVP-023 authorization reserves (`docs/final-decisions.md`, "do not modify completed ... behavior except the Q37 fixes"). Options: (A) approve a one-line fix inside MVP-023 as a separate minimal commit with a regression test that fails first; (B) fix it as its own corrective story first and leave MVP-023 Blocked meanwhile; (C) a CI-only workaround (fewer workers, or a Postgres with more connections) that masks the defect and leaves production exposed. Recommendation: A, because the gate cannot be reliably green without it, the change is one condition in one function, and it removes a real outage risk. Safest reversible default until answered: none of the three is done and MVP-023 stays Blocked. **Evidence added 2026-09-21:** CI run 2 (35666983338, docs-only head 9cd68c5): the same signature. 417 of 420 passed, 3 Chromium failures (an unknown category slug and an unknown product slug returned 500 instead of 404; a session revoke failed), 0 skipped, 0 retries; Firefox and WebKit passed every test; the server log again shows TooManyConnections (8 lines). Whole job 5m57s, test execution 4m14s. The Playwright browser cache is saved only by a successful job, so both runs so far had a cold cache. Proposed fix validated in a throwaway git worktree (never committed, not on the feature branch): one condition in packages/db/src/index.ts (cache the client on globalThis in every environment) plus a unit test (packages/db/src/client-cache.test.ts). The test fails without the fix in production mode (and passes in development and test), and passes with it; with the fix, 40 sequential queries in production mode hold 1 open connection (was 41); and the full accessibility suite against a production build with the fix: 420 passed, 0 failed, 0 skipped, 0 retries (140 per engine) in 7.0m on a local Windows run with 3 workers; TooManyConnections lines in the server log: 0.

## Raised 2026-09-22 (MVP-010 pre-work) — questions only; nothing here is decided
44. **CLOSED (2026-09-22, direct product-owner instruction — `docs/final-decisions.md`, 'MVP-010 open questions 44 and 45').** Require sign-in for all free downloads in MVP-010. The per-product "requires sign-in" policy field is **deferred, not omitted** — a real future story (it needs a second identity model: anonymous ownership, email capture, abuse controls, claim-on-registration, not a schema column), not added now in any form, including disabled or unused. Universal sign-in is stated plainly as this MVP's position, not the intended end state. *Original question:* **Free-download sign-in policy (MVP-010, FR-005).** FR-005 states free downloads "may require sign-in **based on product policy**" — a per-product field, not a blanket rule. No such field exists on `Product` today (`packages/db/prisma/schema/catalog.prisma`, read directly). Building the real per-product policy is not just one column: if sign-in is not required for a given product, the flow has no `User` to attach an `Entitlement` to, which means a second, materially different guest-request code path has to be built and tested alongside the signed-in path, in the same story. Full analysis: `planning/prework/MVP-010-prework-analysis.md`, "Answers to the five questions," item 2.
45. **CLOSED (2026-09-22, direct product-owner instruction — `docs/final-decisions.md`, 'MVP-010 open questions 44 and 45').** Product-scoped (not release-scoped), permanent-until-revoked, **approved with an amendment**: the nullable `revokedAt` column is included AND enforced at read time — a download is denied when it is non-null — even though no code path in this story ever sets it. Rationale recorded: an unenforced reserved column invites a future revocation feature that ships without the check; enforcing the condition now costs one comparison and means product suspension behaves correctly the moment any future write path sets the column. *Original question:* **Free-entitlement revocation (MVP-010, FR-005).** Whether a free `Entitlement` is permanent, revocable if its product is later suspended/unpublished, or version-scoped is not stated by FR-005 and not implied by the existing schema. Full analysis: `planning/prework/MVP-010-prework-analysis.md`, "Answers to the five questions," item 5.

## Raised 2026-09-22 (MVP-020 pre-work) — questions only; nothing here is decided

46. **DIRECTION APPROVED, STILL OPEN (2026-09-22, direct product-owner instruction — `docs/final-decisions.md`, 'MVP-020 open questions 46, 47 and 48').** The proposed per-data-class model is approved as the *recorded direction* for the future erasure story, not as a binding implementation decision: identity data (`User`/`Account`/`Session`) → pseudonymise; commerce/audit-adjacent data (`Entitlement`, `Download`) → retain under a stated lawful basis; the consent/deletion audit trail itself → always retain. **Stays OPEN**, explicitly blocked on question 47 — a jurisdiction decision may require erasure where pseudonymisation is currently proposed, so this direction must not be treated as settled. **Decided now, because it is schema in this story:** `Restrict`/`NO ACTION` (not `Cascade`) on the `userId`/`actorUserId` FKs of `PolicyVersion`, `ConsentRecord`, `DeletionRequest` and `DeletionRequestEvent` — approved. *Original question, deletion-versus-append-only per-data-class treatment:* MVP-020 itself executes no erasure, anonymisation or pseudonymisation of any kind — it only records a deletion *request* and its reviewable state. But CLAUDE.md's standing "audit events are append-only" rule and MVP-010's precedent (`Download` append-only; `Entitlement.userId` cascades on delete) mean the *eventual* treatment of each data class materially affects today's schema. Full analysis: `planning/prework/MVP-020-prework-analysis.md`, "Answers to the seven questions," item 1.
47. **INTERIM DEFAULT APPROVED, STILL OPEN (2026-09-22, direct product-owner instruction — `docs/final-decisions.md`, 'MVP-020 open questions 46, 47 and 48').** A single configurable operational-target value, stored as data, not tied to any named regime — approved. **Stays OPEN**, blocked on items 3 (initial countries/currencies/tax/refunds) and 5 (hosting region/data residency). Binding constraints: no regime name (GDPR, PIPEDA, CCPA or any other) appears anywhere — code, schema, enum values, column names, comments, UI, docs, metadata or commit messages; no timeline is hardcoded; the value is described only as an operational target, never a legal or statutory deadline; nothing in the UI states or implies a legal entitlement, right, or guaranteed timeframe. **Consent categories `TERMS_OF_SERVICE` and `MARKETING_EMAIL` are approved as the MVP set** — excluding analytics consent is correct, since the feature does not exist. *Original question:* which legal regime(s) apply is unknown while items 3 and 5 stay open, and consent categories/retention periods/deletion timelines vary by regime. Full analysis: `planning/prework/MVP-020-prework-analysis.md`, "Answers to the seven questions," item 2.
48. **CLOSED (2026-09-22, direct product-owner instruction — `docs/final-decisions.md`, 'MVP-020 open questions 46, 47 and 48').** Add `ADMIN` to the existing `UserRole` enum — approved, limited to adding the enum value and nothing else (no change to existing role semantics, rows, defaults, or any other part of MVP-002's schema). An allowlist or other interim mechanism was explicitly rejected (a second authorization path/source of truth). Deferring the admin surface entirely was also rejected (a deletion request nobody can action has no exit). Required constraints, binding: no application code path ever grants `ADMIN` (manual operator action against the database only, documented but not tooled); tests create an admin directly in the database under the reserved-prefix pattern; every admin surface is deny-by-default and server-side role-checked, giving a `MEMBER` the same response as an unauthenticated request; every admin transition on a deletion request records actor/timestamp/reason; `ADMIN` is coarse-grained by design and implies no other admin capability elsewhere in the product. *Original question:* MVP-020's scope explicitly includes "the admin surface needed to see/action requests," but `UserRole` had exactly one value, `MEMBER` — there was no way to authorize anyone to review or action another user's deletion request without either a deny-by-default violation or an unapproved mechanism. Full analysis: `planning/prework/MVP-020-prework-analysis.md`, "Answers to the seven questions," item 6.

## Raised 2026-09-22 (MVP-018 pre-work) — questions only; nothing here is decided

49. **CLOSED — option (a), SUBMITTED only (2026-09-22, direct product-owner instruction — `docs/final-decisions.md`, 'MVP-018 open question 49').** Send a deletion-request acknowledgement on `SUBMITTED` only. `UNDER_REVIEW` and `WITHDRAWN` are not approved (no user value / already confirmed in the UI). `APPROVED` and `COMPLETED` are withheld on a **truthfulness ground**: MVP-020 executes no erasure, so a message describing an approved/completed deletion would describe an action the system does not perform — sendable only once a real erasure capability exists and questions 46/47 are settled. `DENIED` is a **recorded known gap, not an omission** — withheld because its copy needs product-owner authorship, not because it lacks value; a future item. Explicitly authorized: adding the send call to MVP-020's `POST /api/account/deletion-requests`, with binding constraints (send after commit, never inside the transaction; a send failure never fails/rolls back the request; no retry; no new `DeletionRequest` column to track send status). Message is transactional (not gated by `MARKETING_EMAIL` consent), states only that the request was received and will be reviewed, and is placeholder copy pending product-owner review — no claim of deletion, timeframe, legal right or regulatory compliance. *Original question:* whether to add deletion-request-lifecycle email notifications, which requires modifying MVP-020's already-completed route files beyond what question 1 (the sign-in-sender migration) requires. Full analysis: `planning/prework/MVP-018-prework-analysis.md`, "Answers to the seven questions," item 2.

## Raised 2026-09-23 (MVP-017 implementation) — questions only; nothing here is decided

50. **OPEN.** `LearningPath`/`LearningPathItem` (FR-014's fifth-minus-collections content type: an ordered sequence of items referencing other published content) are explicitly deferred by MVP-017 (`docs/final-decisions.md`, "MVP-017 implementation: `Article` only this pass; `LearningPath` deferred as a fast-follow within FR-014"). MVP-017 builds `Article` (tutorials, patterns, comparison pages) only. **Not yet decided:** whether `LearningPath`/`LearningPathItem` should be built as their own new backlog story, or as an approved scope extension of MVP-017 itself. Needs a product-owner decision when prioritized. Until then `LearningPath`/`LearningPathItem` stay named, unbuilt placeholders in `docs/06-data-model.md`, alongside `SEORecord`.

## Raised 2026-09-23 (open question 2 decision) — recorded without answering

51. **OPEN — recorded, not answered, and not to be resolved in this repository.** MVP-011 (Creator application) additionally depends on a legal entity existing and on the product owner's employment conflict-of-interest position being confirmed. Neither is an engineering dependency. This is noted here only so a future session does not treat MVP-011 as unblocked once questions 2/3/7/8 are otherwise settled — the entity/conflict-of-interest question is a separate, prior gate.
