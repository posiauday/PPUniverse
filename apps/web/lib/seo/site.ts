/**
 * Site-wide SEO constants (MVP-021). "Power Platform Universe" is the working
 * name already used in page titles; the product name and domain are still open
 * (docs/open-questions.md item 1), so it lives in exactly one place.
 */
export const SITE_NAME = "Power Platform Universe";

export const SITE_DESCRIPTION =
  "A trusted Power Platform ecosystem for reusable assets and technical learning.";

/** Longer descriptions are trimmed for meta/Open Graph tags; the page itself is never truncated. */
export const MAX_META_DESCRIPTION_LENGTH = 300;

export const OPEN_GRAPH_LOCALE = "en_US";
